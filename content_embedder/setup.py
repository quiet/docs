from setuptools import setup

setup(name='content_embedder',
      version='0.1',
      description='Embed content from one markdown into another',
      license='MIT',
      packages=['content_embedder'],
      zip_safe=False)
